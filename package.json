{
  "name": "hullo",
  "version": "0.1.0",
  "description": "",
  "main": "./core",
  "scripts": {
    "index": "./index-folders.sh",
    "clear": "./clean_up_after_build.sh",
    "check": "npm run clear; npm run index; tsc --noEmit",
    "dev": "npm run clear; npm run index; concurrently -i -n tsc,jest \"tsc --noEmit -w\" \"jest --watch\"",
    "build": "npm run clear; npm run index; tsc --noEmit && jest && tsc",
    "prepublish": "npm run build",
    "test": "npm run clear; npm run index; jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^24.0.9",
    "@types/node": "^11.10.4",
    "concurrently": "^4.1.0",
    "jest": "^24.1.0",
    "prettier": "^1.16.4",
    "ts-jest": "^24.0.0",
    "ts-node": "^8.0.2",
    "typescript": "^3.3.3333"
  },
  "dependencies": {}
}
